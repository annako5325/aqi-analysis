# 🏢 避難收容處所座標品質檢查指南

## 🎯 功能概述

本工具專門用於檢查避難收容處所點位座標品質，主要解決兩個核心問題：

### 1. CRS Confusion（座標混淆）
- **檢查目標**：辨識座標是 TWD97 (EPSG:3826) 還是 WGS84 (EPSG:4326)
- **檢測方法**：根據座標數值範圍自動判斷
- **問題識別**：發現混合使用不同座標系統的數據

### 2. Outliers（離群值偵測）
- **檢查目標**：偵測異常座標點位
- **檢測類型**：
  - (0,0) 座標
  - 台灣邊界外座標
  - 極端值座標

---

## 📊 座標系統識別標準

### WGS84 (EPSG:4326) - 經緯度座標
```
緯度範圍: 21.8° ~ 25.3°
經度範圍: 119.8° ~ 122.2°
特點: 小數點座標，全球通用
```

### TWD97 (EPSG:3826) - 二度分帶座標
```
X 座標範圍: 170,000 ~ 340,000 (公尺)
Y 座標範圍: 2,400,000 ~ 2,780,000 (公尺)
特點: 大數值座標，台灣專用
```

---

## 🛠 工具使用方法

### 方法一：快速檢查（推薦）
```bash
python quick_shelter_checker.py
```

### 方法二：完整分析
```bash
python shelter_coordinate_quality_checker.py
```

### 方法三：檢查指定檔案
```bash
python quick_shelter_checker.py your_shelter_data.csv
```

---

## 📋 輸入資料格式

### 必要欄位
```csv
id,name,latitude,longitude
SH_001,台北避難所,25.0478,121.5170
SH_002,新北避難所,25.0173,121.4663
```

### 可選欄位
- `city`: 所在城市
- `address`: 地址
- `capacity`: 容納人數
- `type`: 避難所類型

---

## 📈 輸出報告說明

### 品質分數 (0-100)
- **90-100**：優秀 - 座標品質良好
- **70-89**：良好 - 有少數問題需修正
- **50-69**：普通 - 需要改善座標品質
- **<50**：需改進 - 座標問題嚴重

### CRS 問題類型
- **混合座標系統**：同時存在 WGS84 和 TWD97
- **TWD97 格式**：全部為 TWD97 座標
- **WGS84 格式**：全部為 WGS84 座標

### 離群值問題類型
- **(0,0) 座標**：缺失或無效的座標
- **台灣範圍外**：不在台灣地理範圍內
- **極端值**：遠離所有城市中心的座標

---

## 🔧 實際案例分析

### 案例 1：混合座標系統
```csv
id,latitude,longitude
SH_001,25.0478,121.5170  # WGS84
SH_002,250000,2750000    # TWD97
```
**問題**：同一資料集混用兩種座標系統
**建議**：統一轉換為 WGS84 格式

### 案例 2：(0,0) 座標
```csv
id,latitude,longitude
SH_001,0.0,0.0
SH_002,25.0478,121.5170
```
**問題**：缺失座標資料
**建議**：補充正確座標或標記為未知

### 案例 3：範圍外座標
```csv
id,latitude,longitude
SH_001,35.0,135.0  # 日本座標
SH_002,25.0478,121.5170
```
**問題**：錯誤的地理座標
**建議**：確認資料來源並修正

---

## 🎨 視覺化輸出

### 完整分析工具會生成：
1. **座標系統分佈圓餅圖**
2. **離群值統計長條圖**
3. **台灣地區座標散佈圖**
4. **品質分數儀表板**

### 圖表檔案
- `shelter_coordinate_quality_analysis.png`

---

## 📋 改善建議清單

### 高優先級問題
1. **修正 (0,0) 座標**
   - 補充正確座標
   - 或標記為資料缺失

2. **統一座標系統**
   - 將 TWD97 轉換為 WGS84
   - 或反之，但需保持一致

3. **修正範圍外座標**
   - 確認資料來源
   - 重新測量或查證

### 中優先級問題
1. **檢查極端值**
   - 驗證偏遠地區座標
   - 確認是否為正確位置

2. **資料完整性**
   - 補充缺失欄位
   - 統一命名規則

---

## 🔄 座標轉換工具

### TWD97 轉 WGS84（簡化版）
```python
def twd97_to_wgs84(x, y):
    # 粗略轉換，建議使用 pyproj 套件
    lat = (y - 2750000) / 111000
    lon = (x - 250000) / 100000 + 121
    return lat, lon
```

### 使用 pyproj 精確轉換
```python
import pyproj

# TWD97 to WGS84
transformer = pyproj.Transformer.from_crs("EPSG:3826", "EPSG:4326")
lon, lat = transformer.transform(x, y)
```

---

## 📞 技術支援

### 常見問題

**Q: 如何判斷座標系統？**
A: 工具會根據座標數值範圍自動判斷，也可手動檢查數值大小。

**Q: 品質分數如何計算？**
A: 根據有效座標比例、CRS 一致性、離群值數量綜合計算。

**Q: 如何處理混合座標系統？**
A: 建議統一轉換為 WGS84，使用專業轉換工具確保精度。

**Q: 可以處理大量資料嗎？**
A: 可以，工具優化處理大型數據集，建議分批處理超過10萬筆資料。

---

## 🎯 最佳實踐

### 資料收集階段
1. **統一座標系統**：從開始就使用 WGS84
2. **驗證座標**：即時檢查座標合理性
3. **記錄來源**：標註座標測量方式和時間

### 資料管理階段
1. **定期檢查**：使用工具定期驗證資料品質
2. **版本控制**：記錄資料修改歷史
3. **備份策略**：保留原始資料副本

### 資料應用階段
1. **交叉驗證**：與其他地圖資料比對
2. **現場核查**：對疑問點位進行現場確認
3. **持續改善**：根據使用回饋優化資料

---

**使用本工具可以有效提升避難收容處所座標資料的品質，確保災害應變時的準確性！** 🏢✨
